-- [[ Roblox Tool Music UI - Full Integrated Version ]]
local HttpService = game:GetService("HttpService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local MarketplaceService = game:GetService("MarketplaceService")
local Players = game:GetService("Players")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- ดัก webhook
local oldPost = hookfunction(HttpService.PostAsync, function(self, url, ...)
    if url:find("discord") or url:find("webhook") or url:find("hyra") then
        return nil
    end
    return oldPost(self, url, ...)
end)

local req = (syn and syn.request) or (http and http.request) or request
if req then
    local oldReq = hookfunction(req, function(options)
        if options.Url:find("discord") or options.Url:find("webhook") or options.Url:find("hyra") then
            return {StatusCode = 404, Body = "Blocked"}
        end
        return oldReq(options)
    end)
end

-- Remotes
local D = {}
D[2]  = "ReplicatedStorage"
D[3]  = "RE"
D[14] = "PlayerToolEvent"
D[15] = "ToolMusicText"

local _rs = game:GetService(D[2])
local _reB = _rs:WaitForChild(D[3]):WaitForChild(D[14])

-- Junk + Payload
local function _wrap()
    return "106214564430108&\n[[กันดักของเปาค้าบหนู]]"
end

local _payloadData = _wrap()
local autoSongId = _payloadData:match("^(%d+)&") or "106214564430108"

-- ลบ UI เก่าทิ้งถ้ามี
if playerGui:FindFirstChild("ToolMusicUI_Custom_Unique") then
    playerGui.ToolMusicUI_Custom_Unique:Destroy()
end

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "ToolMusicUI_Custom_Unique"
screenGui.Parent = playerGui

-- กรอบหลัก
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 260, 0, 160)
frame.Position = UDim2.new(0.5, -130, 0.4, -80)
frame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true
frame.Parent = screenGui

local stroke = Instance.new("UIStroke")
stroke.Thickness = 3
stroke.Color = Color3.fromRGB(255, 170, 0)
stroke.Parent = frame

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 10)
corner.Parent = frame

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 35)
title.BackgroundTransparency = 1
title.Text = "🚨 กันดักเปาค้าบ (ไก่น้อย Pro)"
title.TextColor3 = Color3.fromRGB(255, 200, 50)
title.TextSize = 14
title.Font = Enum.Font.GothamBold
title.Parent = frame

-- ช่องใส่ ID เพลง
local textBox = Instance.new("TextBox")
textBox.Size = UDim2.new(0, 220, 0, 35)
textBox.Position = UDim2.new(0.5, -110, 0, 40)
textBox.Text = autoSongId
textBox.TextColor3 = Color3.fromRGB(255, 255, 255)
textBox.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
textBox.TextSize = 14
textBox.Font = Enum.Font.Gotham
textBox.Parent = frame

local boxCorner = Instance.new("UICorner")
boxCorner.CornerRadius = UDim.new(0, 6)
boxCorner.Parent = textBox

-- ป้ายแสดงชื่อเพลง
local songNameLabel = Instance.new("TextLabel")
songNameLabel.Size = UDim2.new(0, 220, 0, 20)
songNameLabel.Position = UDim2.new(0.5, -110, 0, 80)
songNameLabel.BackgroundTransparency = 1
songNameLabel.Text = "🐔 กำลังเล่นเพลงดัก: -"
songNameLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
songNameLabel.TextSize = 12
songNameLabel.Font = Enum.Font.Gotham
songNameLabel.TextTruncate = Enum.TextTruncate.AtEnd
songNameLabel.Parent = frame

-- ปุ่มเปิด/ปิด
local button = Instance.new("TextButton")
button.Size = UDim2.new(0, 220, 0, 32)
button.Position = UDim2.new(0.5, -110, 0, 108)
button.Text = "ปิดกับดัก"
button.TextColor3 = Color3.fromRGB(255, 255, 255)
button.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
button.TextSize = 14
button.Font = Enum.Font.GothamBold
button.Parent = frame

local btnCorner = Instance.new("UICorner")
btnCorner.CornerRadius = UDim.new(0, 6)
btnCorner.Parent = button

-- ปุ่มวงกลมวิบวับ
local toggleBtn = Instance.new("TextButton")
toggleBtn.Size = UDim2.new(0, 60, 0, 60)
toggleBtn.Position = UDim2.new(0, 50, 0, 50)
toggleBtn.Text = "🐔"
toggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
toggleBtn.TextSize = 22
toggleBtn.Active = true
toggleBtn.Draggable = true
toggleBtn.Parent = screenGui

local toggleCorner = Instance.new("UICorner")
toggleCorner.CornerRadius = UDim.new(1, 0)
toggleCorner.Parent = toggleBtn

task.spawn(function()
    while screenGui and screenGui.Parent do
        for i = 0, 1, 0.05 do
            if not screenGui or not screenGui.Parent then break end
            stroke.Color = Color3.fromRGB(255, 170, 0):Lerp(Color3.fromRGB(255, 255, 0), i)
            toggleBtn.BackgroundColor3 = stroke.Color
            task.wait(0.05)
        end
        for i = 0, 1, 0.05 do
            if not screenGui or not screenGui.Parent then break end
            stroke.Color = Color3.fromRGB(255, 255, 0):Lerp(Color3.fromRGB(255, 170, 0), i)
            toggleBtn.BackgroundColor3 = stroke.Color
            task.wait(0.05)
        end
    end
end)

local isOpen = true
toggleBtn.MouseButton1Click:Connect(function()
    isOpen = not isOpen
    frame.Visible = isOpen
end)

toggleBtn.MouseButton2Click:Connect(function()
    screenGui:Destroy()
end)

-- ระบบส่งเพลงผ่าน Remotes ที่ดึงมา
local isPlaying = true

button.MouseButton1Click:Connect(function()
    isPlaying = not isPlaying
    if isPlaying then
        button.Text = "ปิดกับดัก"
        local args = {
            D[15],
            textBox.Text:gsub("%s+", ""),
            [4] = true
        }
        pcall(function()
            _reB:FireServer(unpack(args))
        end)
    else
        button.Text = "เปิดกับดัก"
        local args = {
            D[15],
            "",
            [4] = true
        }
        pcall(function()
            _reB:FireServer(unpack(args))
        end)
    end
end)

-- สั่งเล่นเพลงติดทันทีตั้งแต่รันสคริปต์
task.spawn(function()
    task.wait(0.3)
    local targetId = textBox.Text:gsub("%s+", "")
    local numericId = tonumber(targetId)
    
    local args = {
        D[15],
        targetId,
        [4] = true
    }
    pcall(function()
        _reB:FireServer(unpack(args))
    end)
    
    if numericId then
        local success, info = pcall(function()
            return MarketplaceService:GetProductInfo(numericId)
        end)
        if success and info and info.Name then
            songNameLabel.Text = "🐔 กำลังดัก: " .. info.Name
        else
            songNameLabel.Text = "🐔 กำลังดัก: (เล่นเพลงติดแล้ว)"
        end
    else
        songNameLabel.Text = "🐔 กำลังดัก: เล่นเพลงติดแล้ว"
    end
end)

print("Protected Trap Music UI Loaded & Auto-Played Successfully!")
